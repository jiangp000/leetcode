package ACM;

import java.io.*;
import java.math.*;
import java.util.*;
import java.text.*;

public class acm {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        List<Integer> res = new ArrayList<>();
        HashMap<Integer,String> you = new HashMap<>();
        Queue<Integer> my = new LinkedList<>();
        Arrays.sort(new int[]{1,2,3});
        Collections.sort(res);
        Math.max(1,2);
        System.out.println();
    }
}
